\documentclass[varwidth]{standalone}
\usepackage{amsmath,amssymb}
\begin{document}

\[
a = 1
\]

% 在 `standalone` 文档类里, 不用把 `align` 放在 `$$ ... $$` 里, 直接用 `align` 环境即可.
\begin{align*}
    a &= 1 \\
    b &= 2
\end{align*}

\end{document}


