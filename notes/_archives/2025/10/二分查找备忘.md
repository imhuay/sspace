二分查找相关
===
<!--START_SECTION:badge-->
<!--END_SECTION:badge-->
<!--info
date: 2025-10-28 18:47:32
toc_title: '二分查找相关'
top: false
draft: false
thorough: false
hidden_in_recent: true
section_number: false
omit_in_tag_toc: false
level: 0
tags: [algo_temp]
algo_tags: [二分查找]
-->

<!--START_SECTION:keywords-->
> ***Keywords**: 二分查找备忘*
<!--END_SECTION:keywords-->

<!--START_SECTION:paper_title-->
<!--END_SECTION:paper_title-->

<!--START_SECTION:toc-->
- [基础操作](#基础操作)
    - [左边界查找](#左边界查找)
    - [右边界查找](#右边界查找)
- [经典问题](#经典问题)
    - [旋转数组 📌](#旋转数组-)
        - [旋转数组的最小值](#旋转数组的最小值)
    - [判定型二分](#判定型二分)
- [相关问题](#相关问题)
<!--END_SECTION:toc-->

---

## 基础操作

- 常见有两类模板: **开区间 `[l,r)`** 和 **闭区间 `[l,r]`**;
- **闭区间 `[l,r]`** 📌
    - 区间始终包含两端点, **更直观**;
    - 循环条件: 
        - **收缩到单点 (找最值)**: 
            - `while l < r`, 退出状态: `l == r`
        - **查找目标值**: 
            - `while l ≤ r`, 退出状态: `l > r`
    - 更新规则:
        - `l = mid + 1`
        - `r = mid - 1`
- **开区间 `[l,r)`**
    - 区间不包含右端点 (或左端点);
    - 循环条件: `while l < r`
    - 更新规则:
        - `l = mid + 1`
        - `r = mid`
    - 与多数语言提供的标准库类似的接口, 如 Python, C++;
    - 在解题时可能没那么直观;

### 左边界查找
- 返回 **第一个** `≥ x` 的位置.

<details><summary><b>🔸 闭区间 <code>[l,r]</code></b></summary>

```python
def bisect_left(a, x):
    """"""
    l, r = 0, len(a) - 1            # 闭区间 [0, n-1]
    while l <= r:                   # 结束时 l = r + 1
        mid = l + (r - l) // 2
        if a[mid] < x:
            l = mid + 1
        else:
            r = mid - 1             # 保证闭区间
    return l
```

</details>

<details><summary><b>🔸 开区间 <code>[l,r)</code></b></summary>

```python
def bisect_left(a, x):
    """"""
    l, r = 0, len(a)                # 开区间 [0, n)
    while l < r:                    # 结束时 l = r
        mid = l + (r - l) // 2
        if a[mid] < x:              # 小于目标值时, 往右找
            l = mid + 1
        else:
            r = mid
    return l
```
> _参考 `from bisect import bisect_left`_

</details>

> [35. 搜索插入位置 - 力扣（LeetCode）](https://leetcode.cn/problems/search-insert-position/)

> [34. 在排序数组中查找元素的第一个和最后一个位置 - 力扣（LeetCode）](https://leetcode.cn/problems/find-first-and-last-position-of-element-in-sorted-array/)


---

### 右边界查找

- 返回 **第一个** `> x` 的位置.
    > ⚠️ _注意是 `>`, 是不包括 `x` 的._
- 与 **左边界查找** 只有一个区别: `if a[mid] < x` → `if a[mid] <= x`

<details><summary><b>🔸 闭区间 <code>[l,r]</code></b></summary>

```python
def bisect_right(a, x):
    """"""
    l, r = 0, len(a) - 1
    while l <= r:
        mid = l + (r - l) // 2
        if a[mid] <= x:         # 小于等于目标值时, 往右找
            l = mid + 1
        else:
            r = mid - 1
    return l
```

</details>

<details><summary><b>🔸 开区间 <code>[l,r)</code></b></summary>

```python
def bisect_right(a, x):
    """"""
    l, r = 0, len(a)
    while l < r:
        mid = l + (r - l) // 2
        if a[mid] <= x:         # 小于等于目标值时, 往右找
            l = mid + 1
        else:
            r = mid
    return l
```
> _参考 `from bisect import bisect_right`_

</details>

---

## 经典问题

### 旋转数组 📌

> 🚨 **备忘**: 旋转数组相关的 4 道题, 目前使用的模板是在测试了各种模板后, 保留下来改动最小的版本 (统一使用 **闭区间** `[l, r]` 和 `nums[m] > nums[r]` 判断左侧有序); 
> **其他模板总会一些特殊用例无法通过, 太耗费精力, 不再深入研究, 记录以备忘**.

- **旋转数组特性**: 
    - 数组被分为两段有序区间;
        $$\lbrack a_k, a_{k+1}, \cdots, a_{n-1} \rbrack \lbrack a_0, a_1, \cdots, a_{k-1} \rbrack \ , \quad a_{k-1} \le a_k$$
    
- **思路**:
    - 关键: 判断中点 `nums[m]` 落在哪段有序区间;
        - ~~**如果 `nums[l] <= nums[m]`, 则左侧有序**, 检查目标是否在 `[l, m)` 内;~~
            > ~~⚠️ 如果数组 **无重复**, `<` 也可以; 但如果 **有重复**, 必须 `<=`.~~
        - 当 `nums[m] > nums[r]` (或 `nums[l] <= nums[m]`) 时, 左侧一定有序, 检查目标是否在 `[l, m)` 内;
        - 反之, 右侧有序, 检查目标是否在 `[m, r)` 内.

<details><summary><b>🔸 搜索旋转排序数组 (无重复, 闭区间实现)</b></summary>

> [33. 搜索旋转排序数组 - 力扣（LeetCode）](https://leetcode.cn/problems/search-in-rotated-sorted-array/)

**闭区间 `[l, r]`**
```python
l, r = 0, len(nums) - 1

while l <= r:
    m = (l + r) // 2

    if nums[m] == target:
        return m

    if nums[m] > nums[r]:     # 此时, 左侧一定有序
        # 左侧有序
        if nums[l] <= target < nums[m]:
            r = m - 1
        else:
            l = m + 1
    else:
        # 右侧有序
        if nums[m] < target <= nums[r]:
            l = m + 1
        else:
            r = m - 1

return -1
```

</details>

<details><summary><b>🔸 搜索旋转排序数组 (有重复, 闭区间实现)</b></summary>

> [81. 搜索旋转排序数组 II - 力扣（LeetCode）](https://leetcode.cn/problems/search-in-rotated-sorted-array-ii/)

- **思路**:
    - 在旋转排序数组中搜索的关键是判断哪边有序;
    - 在有重复元素的旋转排序数组中, **有且只有一种情况无法判断哪边有序**, 即 `nums[l] == nums[m] == nums[r-1]` 时;
        - `nums[l] != nums[m], nums[m] != nums[r-1]` 时, 普通情况, 略;
        - `nums[l] == nums[m], nums[m] != nums[r-1]` 时, 左侧一定有序;
            - `nums[l] == nums[m]` 只有两种情况: 
                1. `m` 落在左侧有序区间, `[l,m]` 区间内全等;
                2. `m` 落在右侧有序区间, 此时一定有 `nums[m] == nums[r-1]`, 矛盾;
        - 同理, `nums[l] != nums[m], nums[m] == nums[r-1]` 时, 右侧一定有序.
    - 当无法判断哪边有序时, **收缩区间**
        - `nums[l] == nums[m] == nums[r-1]` 有且只有两种情况:
            ```txt
            a a a b c a a a a a a a a
            l →         m         ← r

            a a a a a a a a b c a a a
            l →         m         ← r
            ```
        - 操作方法相同:
            - `l += 1`
            - `r -= 1`

**闭区间 `[l, r]`**
```python
l, r = 0, len(nums) - 1

while l <= r:
    m = (l + r) // 2

    if nums[m] == target:
        return True
    
    if nums[l] == nums[m] == nums[r]:   # 收缩区间
        l += 1
        r -= 1
    elif nums[m] > nums[r]:             # 此时, 左侧一定有序 (收缩区间后)
        if nums[l] <= target < nums[m]:
            r = m - 1
        else:
            l = m + 1
    else:                               # 右侧一定有序
        if nums[m] < target <= nums[r]:
            l = m + 1
        else:
            r = m - 1

return False
```

</details>

---

#### 旋转数组的最小值

- **旋转数组**:
    $$\lbrack a_k, a_{k+1}, \cdots, a_{n-1} \rbrack \lbrack a_0, a_1, \cdots, a_{k-1} \rbrack \ , \quad a_{k-1} \le a_k$$

- **思路**:
    - 最小值一定在右侧有序区间, 即 $a_0$;
    - 中点落到左侧时, 就移动 `l → m + 1`;
    - ⚠️ 注意, **中点落到右侧时**, 因为不能排除 `nums[m]` 是最小值的情况, 所以不能直接移动 `r → m - 1`, 只能 `r → m`.

<details><summary><b>🔸 寻找旋转排序数组中的最小值 (无重复, 闭区间实现)</b></summary>

> [153. 寻找旋转排序数组中的最小值 - 力扣（LeetCode）](https://leetcode.cn/problems/find-minimum-in-rotated-sorted-array/)

**闭区间 `[l, r]`**
```python
l, r = 0, len(nums) - 1

while l < r:
    # 如果当前区间整体有序, 直接返回最左端
    if nums[l] < nums[r]:
        return nums[l]

    m = (l + r) // 2

    if nums[m] > nums[r]:  # 左侧一定有序, 最小值在右侧
        l = m + 1
    else:
        r = m              # 注意这里不能 - 1

return nums[l]
```
- `nums[m] <= nums[r]` (即右侧有序时), 不能排除 `nums[m]` 是最小值的情况, 所以不能直接把 `r` 移动到 `m - 1`, 只能移动到 `m`;

</details>

<details><summary><b>🔸 寻找旋转排序数组中的最小值 (有重复, 闭区间实现)</b></summary>

> [154. 寻找旋转排序数组中的最小值 II - 力扣（LeetCode）](https://leetcode.cn/problems/find-minimum-in-rotated-sorted-array-ii/)

**闭区间 `[l, r]`**
```python
l, r = 0, len(nums) - 1

while l < r:
    # 如果当前区间整体有序, 直接返回最左端
    if nums[l] < nums[r]:
        return nums[l]

    m = (l + r) // 2
    if nums[m] > nums[r]:
        l = m + 1
    elif nums[m] < nums[r]:
        r = m               # 这里不能 - 1
    else:
        r -= 1

return nums[l]
```

</details>

### 判定型二分

> ##### TODO

- 在 **二分查找** 的模版上, 添加判定函数;
    ```python
    def bisect_left(a, x):
        """"""
        l, r = 0, len(a)
        while l < r:
            mid = l + (r - l) // 2
            if check(a[mid]) < x:    # 判断是否满足条件
                l = mid + 1
            else:
                r = mid
        return l
    ```

---



---

## 相关问题
<!--START_SECTION:related_problems-->
<!--END_SECTION:related_problems-->