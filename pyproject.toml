[project]
name = "huaytools"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "imhuay", email = "imhuay@163.com" }
]
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "dowhen>=0.1.0",
    "fire>=0.7.0",
    "markdown-it-py>=4.0.0",
    "pyyaml>=6.0.2",
    "openai>=1.102.0",
    "transformers>=4.56.1",
    "emoji>=2.14.1",
    "regex>=2025.9.1",
]

[tool.uv]
index-url = "https://pypi.tuna.tsinghua.edu.cn/simple"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.scripts]
huaytools = "huaytools:main"

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"

[tool.uv.sources]
torch = { index = "pytorch-cpu" }

[project.optional-dependencies]
cpu = [
    "torch>=2.9.0",
]

[dependency-groups]
cpu = [
    "einops>=0.8.1",
    "torch>=2.9.0",
]
dev = [
    "jupyter>=1.1.1",
    "markdown>=3.8.2",
]
lint = []
